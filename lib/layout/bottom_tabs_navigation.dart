import 'package:flutter/material.dart';

class BottomTabsNavigation extends StatelessWidget {
  const BottomTabsNavigation({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('lala');
  }
}
